---
import CollectionLayout from "@components/glossary/CollectionLayout.astro";
import { getTaxaMultiset, getTaxa } from "@lib/taxonomyParser";
import { getEntries, getIndex } from "@lib/contentParser";
import { sortByDate } from "@lib/sortFunctions";
import type { GlossaryEntry } from "@/types";

const { slug } = Astro.params;
const entryIndex = (await getIndex("glossary")) as GlossaryEntry;
const entries = (await getEntries("glossary", sortByDate)) as GlossaryEntry[];
const allCategories = await getTaxaMultiset("glossary", "categories");
const categories = await getTaxa("glossary", "categories");
const tags = await getTaxa("glossary", "tags");

const entriesPerPage = 8;
const currentPageIndex = slug && !isNaN(Number(slug)) ? Number(slug) : 1;
const pageCount = Math.ceil(entries.length / entriesPerPage);
const indexOfLastEntry = currentPageIndex * entriesPerPage;
const indexOfFirstEntry = indexOfLastEntry - entriesPerPage;
const currentEntries = entries.slice(indexOfFirstEntry, indexOfLastEntry);

export async function getStaticPaths() {
  const entriesPerPage = 8;
  const entries = (await getEntries("glossary", sortByDate)) as GlossaryEntry[];
  const pageCount = Math.ceil(entries.length / entriesPerPage);
  const paths = [];

  for (let i = 1; i < pageCount; i++) {
    paths.push({
      params: {
        slug: (i + 1).toString(),
      },
    });
  }
  return paths;
}
---

<CollectionLayout
  entryIndex={entryIndex}
  entries={currentEntries}
  categories={categories}
  tags={tags}
  allCategories={allCategories}
  pageIndex={currentPageIndex}
  pageCount={pageCount}
  collection="glossary"
/>
